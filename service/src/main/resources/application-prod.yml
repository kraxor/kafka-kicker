quarkus:
  # noinspection YAMLSchemaValidation
  devservices:
    enabled: false
  datasource:
    db-kind: postgresql
    username: postgres
    password: insecure
    reactive:
      username: postgres
      password: insecure
      url: vertx-reactive:postgresql://localhost:5432/kafka_kicker
    jdbc:
      enabled: false
